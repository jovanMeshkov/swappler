@using Swappler.Utilities
@using Swappler.Models

@model List<SwapItem>

    @foreach (SwapItem item in Model)
    {
        
        <div class="col-sm-3" style="box-shadow:5px 5px 5px #d8d6d6; background-color:white; padding-top:8px; margin-left: 15px; margin-right:15px; ">
                    <h3>
                        @item.Name
                    </h3>
                    <div class="swap-item-date" >
                                    @item.Date
                    </div>
            <div class="col-item">
                <div class="photo">
                    <img src="@item.PhotoUrl" class="img-responsive" alt="" style="border: 2px solid orange; border-radius:5px; width:275px; height:205px;" />
                </div>
                <br />
                <div class="info">
                    <div class="separator clear-left">
                        <p class="btn-details">
                            <a href="#" class="hidden-sm">More details</a>
                        </p>

                    </div>
                    
                </div>
            </div>
        </div>
        
    }
